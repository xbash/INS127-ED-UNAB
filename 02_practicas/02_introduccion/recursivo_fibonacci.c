#include <stdio.h>
#include <stdlib.h>

long fibonacci(int n);

//Se puede compilar en la consola con: gcc -Wall -ansi -pedantic namefile.c -o namefile

int main(){
  int n, k;
  do{
    printf("Introduzca el numero de terminos: ");
    scanf("%d", &n);
  }while(n<=1);
  puts("Serie numeros de fibonacci");
  printf("0 1 ");
  for(k=2; k<=n; k++){

  }
  }
}
