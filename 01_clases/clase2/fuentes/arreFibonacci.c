/*
Desarrolle un funcion que reciba un entero n y devueva un arreglo de largo n con
los primers n numeros de Fibonacci. Implemente ademas el main asociado
*/

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){
    int i, n;

    //leer n
    printf("Ingrese el valor de n: ");
    scanf("%d", &n);
    if(n <= 0){
        printf("\n Valor de n es %d no es posible crear el arreglo", n);
    } else {
    //inserta valores fibonacci al arreglo
    int fib[n]; //arreglo de largo fijo
    for()
    }
}
